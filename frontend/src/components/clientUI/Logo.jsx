import logo from "../../assets/imgs/logo.png";

export const Logo = ({ width, height }) => (
    <img 
        src={logo} 
        alt="ArgYou Logo"
        width={width}
        height={height}
    />
);